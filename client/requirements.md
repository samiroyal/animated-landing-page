## Packages
(none needed)

## Notes
Frontend-only marketing landing page — do not call any /api endpoints.
Uses existing framer-motion already installed for scroll/entry animations.
SEO meta tags should be set in client/index.html (not generated here due to constraints); if not updated, app still functions.
